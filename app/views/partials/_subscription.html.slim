= link_to 'Subscribe to the question', subscriptions_path(subscription: {question_id: @question}), method: :post
= link_to 'Unsubscribe to the question', subscription_path(params: {subscription_id: @question.subscriptions.where(user_id: current_user.id).first.id }), method: :delete
